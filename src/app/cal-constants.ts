class CalConstants {
  // 화면 관련
  static VIEW_WIDTH: number = 200;
  static DEFAULT_VIEW_HEIGHT: number = 20;
  static DISPLAY_VIEW_HEIGHT: number = 28;

  // 폰트 관련
  static DEFAULT_FONT_SIZE: number = 13;
  static DEFAULT_FONT: string = `${CalConstants.DEFAULT_FONT_SIZE}px Arial`;

  static DISPLAY_FONT_SIZE: number = 24;
  static DISPLAY_FONT: string = `${CalConstants.DISPLAY_FONT_SIZE}px Arial`;

  // 여백 관련
  static TEXT_BOTTOM_SPACING: number = 2;
}

export default CalConstants;